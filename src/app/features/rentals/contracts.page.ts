import{ChangeDetectionStrategy,Component,computed,inject,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import type{Contract}from'../../core/models/domain.models';import{AppStore}from'../../core/services/app-store.service';import{EntityModalService}from'../../core/services/entity-modal.service';import{brl,normalizeSearch}from'../../core/utils/domain.utils';import{DetailDrawerComponent,type DetailRow}from'../../shared/components/detail-drawer.component';import{EmptyStateComponent}from'../../shared/components/empty-state.component';import{PageHeadingComponent}from'../../shared/components/page-heading.component';
@Component({selector:'app-contracts-page',imports:[FormsModule,PageHeadingComponent,EmptyStateComponent,DetailDrawerComponent],changeDetection:ChangeDetectionStrategy.OnPush,template:`<section class="page-enter"><app-page-heading title="Contratos" description="Locações ativas, vigências, valores e regras de cobrança." actionLabel="Novo contrato" (action)="modal.open('contract')"/><div class="contract-overview"><div class="contract-overview-intro"><span>Base contratual</span><strong>{{store.contracts().length}} contratos ativos</strong><small>Vínculos vigentes no portfólio.</small></div><div class="contract-overview-billing"><span>Aluguel mensal</span><strong>{{money(totalRent())}}</strong><small>receita contratada</small></div><div><span>Unidades vinculadas</span><strong>{{linkedUnits()}}</strong><small>espaços locados</small></div><div><span>Ticket médio</span><strong>{{money(averageRent())}}</strong><small>por contrato</small></div></div><section class="table-section"><div class="table-toolbar"><label class="search-field"><span class="app-search-icon">⌕</span><input [(ngModel)]="query" placeholder="Buscar contrato, locatário ou imóvel"></label><label class="filter-select"><select [(ngModel)]="portfolio"><option value="">Todas as carteiras</option>@for(p of store.portfolios();track p.id){<option [value]="p.name">{{p.name}}</option>}</select><i class="filter-select-chevron"></i></label></div>@if(filtered().length){<div class="contract-card-grid">@for(item of filtered();track item.id){<button class="contract-card" (click)="selected.set(item)"><i class="contract-card-accent"></i><span class="contract-card-head"><span><b>{{item.id}}</b><small>{{item.portfolio}}</small></span><i class="contract-active-badge">Ativo</i></span><span class="contract-card-tenant"><small>Locatário</small><strong>{{item.tenant}}</strong><span>{{item.property}}</span></span><span class="contract-card-units"><small>Unidades vinculadas</small><span>@for(unit of item.units;track unit){<b>{{unit}}</b>}</span></span><span class="contract-card-value"><span><small>Aluguel mensal</small><strong>{{money(item.rent)}}</strong></span><span><small>Vencimento</small><strong>Dia {{item.due}}</strong></span></span><span class="contract-card-period"><span><i></i><b>{{item.period}}</b></span></span><span class="contract-card-footer">{{item.charges.join(' · ')}}<b>Ver contrato <i>→</i></b></span></button>}</div>}@else{<app-empty-state title="Nenhum contrato encontrado" description="Ajuste a busca ou cadastre um contrato."/>}<footer class="table-footer"><span>Contratos vigentes</span><span>{{filtered().length}} registros</span></footer></section></section>@if(selected();as item){<app-detail-drawer eyebrow="Contrato de locação" [title]="item.id" [subtitle]="item.tenant" [rows]="rows(item)" actionLabel="Gerar cobrança" (closed)="selected.set(null)" (action)="modal.open('charge',null,item);selected.set(null)"/>}`})export class ContractsPage{readonly store=inject(AppStore);readonly modal=inject(EntityModalService);readonly selected=signal<Contract|null>(null);query='';portfolio='';readonly totalRent=computed(()=>this.store.contracts().reduce((s,c)=>s+c.rent,0));readonly averageRent=computed(()=>this.totalRent()/Math.max(1,this.store.contracts().length));readonly linkedUnits=computed(()=>this.store.contracts().reduce((s,c)=>s+c.units.length,0));readonly filtered=computed(()=>{const q=normalizeSearch(this.query);return this.store.contracts().filter(c=>(!this.portfolio||c.portfolio===this.portfolio)&&(!q||normalizeSearch(`${c.id} ${c.tenant} ${c.property} ${c.units.join(' ')}`).includes(q)))}) ;money(v:number){return brl.format(v)}rows(c:Contract):DetailRow[]{return[{label:'Carteira',value:c.portfolio},{label:'Imóvel',value:c.property},{label:'Unidades',value:c.units.join(', ')},{label:'Locatário',value:c.tenant},{label:'Vigência',value:c.period},{label:'Aluguel',value:this.money(c.rent)},{label:'Vencimento',value:`Dia ${c.due}`},{label:'Reajuste',value:c.adjustment},{label:'Encargos',value:c.charges.join(', ')}]}}
