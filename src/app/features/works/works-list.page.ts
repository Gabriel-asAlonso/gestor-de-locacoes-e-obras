import{ChangeDetectionStrategy,Component,computed,inject}from'@angular/core';import{FormsModule}from'@angular/forms';import{Router}from'@angular/router';import{FALLBACK_PROPERTY_IMAGE,PROPERTY_IMAGES}from'../../core/data/demo-data';import type{WorkRecord}from'../../core/data/works.data';import{AppStore}from'../../core/services/app-store.service';import{brl,normalizeSearch}from'../../core/utils/domain.utils';import{EmptyStateComponent}from'../../shared/components/empty-state.component';import{PageHeadingComponent}from'../../shared/components/page-heading.component';import{StatusBadgeComponent}from'../../shared/components/status-badge.component';
@Component({selector:'app-works-list-page',imports:[FormsModule,PageHeadingComponent,EmptyStateComponent,StatusBadgeComponent],changeDetection:ChangeDetectionStrategy.OnPush,template:`<section class="page-enter works-list-page"><app-page-heading eyebrow="Módulo de obras" title="Todas as obras" description="Portfólio completo de intervenções, prazos e orçamento." actionLabel="Nova obra" (action)="go('/obras/nova')"/><div class="works-list-overview"><div><span>Obras cadastradas</span><strong>{{store.works().length}}</strong><small>{{activeCount()}} em acompanhamento</small></div><div><span>Orçamento total</span><strong>{{money(totalBudget())}}</strong><small>investimento previsto</small></div><div><span>Realizado</span><strong>{{money(totalSpent())}}</strong><small>{{spentPercent()}}% do orçamento</small></div><div><span>Em atenção</span><strong>{{attentionCount()}}</strong><small>risco ou atraso</small></div></div><section class="table-section"><div class="table-toolbar"><label class="search-field"><span class="app-search-icon">⌕</span><input [(ngModel)]="query" placeholder="Buscar obra, imóvel ou responsável"></label><label class="filter-select"><select [(ngModel)]="status"><option value="">Todas as situações</option><option>Planejada</option><option>Em andamento</option><option>Pausada</option><option>Concluída</option></select><i class="filter-select-chevron"></i></label><label class="filter-select"><select [(ngModel)]="risk"><option value="">Todos os riscos</option><option>Dentro do prazo</option><option>Atenção</option><option>Em atraso</option><option>Concluída</option></select><i class="filter-select-chevron"></i></label></div>@if(filtered().length){<div class="app-work-card-grid">@for(work of filtered();track work.id){<article class="app-work-card"><button (click)="open(work)"><span class="app-work-card-media"><img [src]="image(work)" [alt]="work.property"><i>{{work.id}}</i><app-status-badge [value]="work.status"/></span><span class="app-work-card-body"><small>{{work.property}}{{work.unit?' · '+work.unit:''}}</small><strong>{{work.title}}</strong><span>{{work.manager}} · conclusão {{work.endLabel}}</span><i><b [style.width.%]="work.progress"></b></i><em>{{work.progress}}% concluído</em><span class="app-work-values"><b>{{money(work.spent)}} realizados</b><b>{{money(work.budget)}} orçamento</b></span></span></button><footer><span class="app-risk app-risk-{{riskClass(work)}}">{{work.risk}}</span><button (click)="go('/obras/'+work.id+'/editar')">Editar</button></footer></article>}</div>}@else{<app-empty-state title="Nenhuma obra encontrada" description="Ajuste os filtros ou cadastre uma nova obra."/>}<footer class="table-footer"><span>Portfólio de intervenções</span><span>{{filtered().length}} obras</span></footer></section></section>`})export class WorksListPage{readonly store=inject(AppStore);private readonly router=inject(Router);query='';status='';risk='';readonly filtered=computed(()=>{const q=normalizeSearch(this.query);return this.store.works().filter(w=>(!this.status||w.status===this.status)&&(!this.risk||w.risk===this.risk)&&(!q||normalizeSearch(`${w.id} ${w.title} ${w.property} ${w.manager}`).includes(q)))});readonly activeCount=computed(()=>this.store.works().filter(w=>!['Concluída','Cancelada'].includes(w.status)).length);readonly totalBudget=computed(()=>this.store.works().reduce((s,w)=>s+w.budget,0));readonly totalSpent=computed(()=>this.store.works().reduce((s,w)=>s+w.spent,0));readonly spentPercent=computed(()=>Math.round(this.totalSpent()/Math.max(1,this.totalBudget())*100));readonly attentionCount=computed(()=>this.store.works().filter(w=>['Atenção','Em atraso'].includes(w.risk)).length);money(v:number){return brl.format(v)}riskClass(w:WorkRecord){return w.risk==='Em atraso'?'danger':w.risk==='Atenção'?'warning':'ok'}image(w:WorkRecord){const p=this.store.properties().find(p=>p.name===w.property);return p?PROPERTY_IMAGES[p.id]??FALLBACK_PROPERTY_IMAGE:FALLBACK_PROPERTY_IMAGE}open(w:WorkRecord){this.go('/obras/'+w.id)}go(url:string){void this.router.navigateByUrl(url)}}
